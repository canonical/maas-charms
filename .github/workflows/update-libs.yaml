name: Auto-update Charm Libraries
on:
  # Manual trigger
  workflow_dispatch:
  # Check regularly the upstream every four hours
  schedule:
    - cron: "0 0,4,8,12,16,20 * * *"

jobs:
  update-lib-region:
    name: Check libraries
    uses: canonical/observability/.github/workflows/charm-update-libs.yaml@a02e0296e0ded9def59410b5b100f6c7b10f4dd4
    secrets: inherit
    with:
      charm-path: maas-region

  update-lib-agent:
    name: Check libraries
    uses: canonical/observability/.github/workflows/charm-update-libs.yaml@a02e0296e0ded9def59410b5b100f6c7b10f4dd4
    secrets: inherit
    with:
      charm-path: maas-agent
