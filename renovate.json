{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "schedule": ["every week"],
  "ignoreDeps": [
    "canonical/operator-workflows",
    "tj-actions/changed-files"
  ],
  "packageRules": [
    // Uncomment to batch all updates into a single weekly PR:
    // { "groupName": "weekly dependencies", "matchPackageNames": ["*"] },
    {
      "description": "Update botocore and boto3 together",
      "matchPackageNames": ["botocore", "boto3"],
      "groupName": "botocore"
    }
  ]
}
